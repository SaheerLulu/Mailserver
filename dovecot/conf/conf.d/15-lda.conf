# --- Local delivery / LMTP behaviour ----------------------------------------
protocol lmtp {
  # Run incoming mail through Sieve (so spam gets filed into Junk, etc).
  mail_plugins = $mail_plugins sieve
}

protocol imap {
  mail_plugins = $mail_plugins imap_quota
  imap_idle_notify_interval = 29 mins
}
