# --- Quota -------------------------------------------------------------------
# The actual limit per user comes from the SQL user_query (quota_rule).
# 0 bytes = unlimited.
plugin {
  quota = maildir:User quota
  quota_grace = 10%
}
