# --- Authentication ---------------------------------------------------------
# Never accept plaintext credentials over an unencrypted connection.
disable_plaintext_auth = yes
auth_mechanisms = plain login

# Virtual users live in PostgreSQL.
!include auth-sql.conf.ext
